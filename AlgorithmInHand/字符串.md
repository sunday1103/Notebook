# C语言，处理字符串
```C++
#include <cstring>
int main(int argc, char **argv)
{
    char s[100];      //预先分配一个字符数组
    if (argc > 1)
    {
        cout << argv[1] << endl;
        strcpy(s, argv[1]); //需要使用库函数
        //    s = argv[1]; //这样是不行的，
    }
}
```
