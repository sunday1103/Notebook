// sort algorithm example
#include <algorithm> // std::sort
#include <iostream>  // std::cout
#include <vector>    // std::vector

using namespace std;

int main()
{
    int N;
    cin >> N;
    vector<int> v;
    v.resize(N);

    int a;
    while (scanf("%d", a))
    {
        cout <<
    }
    cout << " output\n";
    for (int i = 0; i < N; ++i)
    {
        cout << v[i] << " ";
    }
    cout << endl;
    return 0;
}